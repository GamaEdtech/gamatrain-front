#!/usr/bin/env sh

# Check if npm is available
if command -v npm >/dev/null 2>&1; then
  npm run lint || exit 1
  npm run check-paths || exit 1
else
  echo "npm not found, skipping pre-commit checks"
  echo "Please run 'npm run lint && npm run check-paths' manually"
fi
